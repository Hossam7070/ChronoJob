# Test Environment Configuration for ChronoJob
# Email sending is disabled in test mode

# SMTP Configuration (not used in test mode)
SMTP_SERVER=smtp.example.com
SMTP_PORT=587
SMTP_USERNAME=test@example.com
SMTP_PASSWORD=test_password
SENDER_EMAIL=chronojob@example.com

# Application Settings
ENVIRONMENT=test
LOG_LEVEL=INFO
